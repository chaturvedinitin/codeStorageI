#include<iostream>
using namespace std;
int main(){
    //------------------------
    /*
    NULL Pointer a pointer that doesn't point to anything
    int* ptr  = NULL;
    */

    /*Dangling pointer -> a pointer that is pointing in a memory location that has either been deleted or freed
    */

    /*
    Void pointer -> a pointer that is not associated with any data type 
    void* ptr
    */

    /*
    wild pointer -> pointer that has predictable behaviour 
    because it hasn't been initialized
    int * ptr;
    */
}